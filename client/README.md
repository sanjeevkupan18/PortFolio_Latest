# Client

Vite + React frontend for the portfolio.

## Environment

Create `client/.env` from `client/.env.example`.

- `VITE_API_BASE_URL`: Backend base URL for deployed builds.
- `VITE_API_PROXY_TARGET`: Local backend URL used by the Vite dev proxy.

## Commands

```bash
npm run dev
npm run build
npm run preview
```
